<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.book.dao.AdminDao">


 <select id="queryAdmin" resultType="Integer" parameterType="String" >
     SELECT id from admin where userName = #{userName} and passwd = #{passwd}
 </select>

</mapper>