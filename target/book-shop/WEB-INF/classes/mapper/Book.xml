<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.book.dao.BookDao">



    <select id="query" resultType="bookDetail" >
      SELECT book.id , book.name , book.price , book.num , book.image_Url , book.description , category.name categoryName  from book join category on book.category_id = category.id;
    </select>

    <insert id="addBook" parameterType="book">

        insert into book  (name ,price, category_id, num ,image_url , description )
        values (#{name}, #{price}, #{categoryId} , #{num} ,  #{imageUrl} , #{description});
    </insert>

</mapper>